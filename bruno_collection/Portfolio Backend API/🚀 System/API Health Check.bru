meta {
  name: API Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health/
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  # Health Check
  Checks the operational status of the API.
  
  **Route:** `GET /api/health`
}
