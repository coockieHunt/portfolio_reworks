meta {
  name: Get Posts offset
  type: http
  seq: 2
}

post {
  url: {{base_url}}/blog/offset
  body: json
  auth: inherit
}

body:json {
  {
    "min": 1,
    "max": 4
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Get Posts
  Retrieves a list of blog posts with pagination.
  
  **Route:** `POST /api/blog/`
}
