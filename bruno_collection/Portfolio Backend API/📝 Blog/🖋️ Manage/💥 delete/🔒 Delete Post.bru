meta {
  name: 🔒 Delete Post
  type: http
  seq: 2
}

delete {
  url: {{base_url}}{{endPointApi}}/blog/{{slugBlog_demoName}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Delete Post
  Permanently removes a blog post from the database.
  
  **Access:** Restricted 🔒
  **Route:** `DELETE /api/blog/:slug`
  
  Deletes the post and automatically clears its Redis cache.
  This action cannot be undone.
}
