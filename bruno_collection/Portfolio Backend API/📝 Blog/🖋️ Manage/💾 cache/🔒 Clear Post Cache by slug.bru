meta {
  name: 🔒 Clear Post Cache by slug
  type: http
  seq: 2
}

delete {
  url: {{base_url}}/blog/cache/delete/{{slugBlog_demoName}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Clear Cache by Slug
  Clears the cache for a specific blog post by its slug.
  
  **Access:** Restricted 🔒
  **Route:** `DELETE /api/blog/cache/delete/:slug`
}
