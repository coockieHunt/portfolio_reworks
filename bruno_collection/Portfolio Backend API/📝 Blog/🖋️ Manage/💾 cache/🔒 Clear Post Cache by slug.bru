meta {
  name: 🔒 Clear Post Cache by slug
  type: http
  seq: 2
}

delete {
  url: {{base_url}}{{endPointApi}}/blog/cache/delete/{{slugBlog_demoName}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Clear Cache by Slug
  Removes a specific blog post from Redis cache.
  
  **Access:** Restricted 🔒
  **Route:** `DELETE /api/blog/cache/delete/:slug`
  
  Forces fresh database query on next request.
  Useful after manual database updates.
}
