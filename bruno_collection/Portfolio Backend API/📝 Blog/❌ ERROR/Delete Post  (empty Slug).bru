meta {
  name: Delete Post  (empty Slug)
  type: http
  seq: 2
}

delete {
  url: {{base_url}}{{endPointApi}}/blog/invalid
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Delete Post (Empty/Invalid Slug)
  Tests the error response when attempting to delete a post with an invalid or empty slug.
  
  **Route:** `DELETE /api/blog/:slug`
}
