meta {
  name: invalid Slug
  type: http
  seq: 1
}

delete {
  url: {{base_url}}/blog/cache/delete/invalid
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Invalid Slug (Cache Delete)
  Tests the error response when attempting to delete cache for an invalid slug.
  
  **Route:** `DELETE /api/blog/cache/delete/:slug`
}
