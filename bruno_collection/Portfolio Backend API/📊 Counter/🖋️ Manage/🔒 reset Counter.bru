meta {
  name: 🔒 reset Counter
  type: http
  seq: 3
}

post {
  url: {{base_url}}{{endPointApi}}/counter/reset/THEME_RAND
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Reset Counter
  Sets a Redis counter value back to zero.
  
  **Access:** Restricted 🔒
  **Route:** `POST /api/counter/reset/:key`
  
  Replace `:key` with the counter name.
  
  Useful for resetting analytics or temporary counters.
}
