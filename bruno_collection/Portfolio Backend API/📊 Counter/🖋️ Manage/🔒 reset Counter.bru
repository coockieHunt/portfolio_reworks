meta {
  name: 🔒 reset Counter
  type: http
  seq: 3
}

post {
  url: {{base_url}}/counter/reset/THEME_RAND
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Reset Counter
  Resets the value of a counter to zero.
  
  **Access:** Restricted 🔒
  **Route:** `POST /api/counter/reset/:value`
}
