meta {
  name: LOGOUT
  type: http
  seq: 2
}

post {
  url: {{base_url}}{{endPointApi}}/auth/logout
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Logout
  Revokes the current JWT token and logs out the user.
  
  **Access:** Requires authentication 🔒
  **Route:** `POST /api/auth/logout`
  
  The token is added to Redis blacklist with configured TTL.
}
