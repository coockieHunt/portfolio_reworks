meta {
  name: 🔒 clear url cache
  type: http
  seq: 4
}

delete {
  url: {{base_url}}{{endPointApi}}/cloudinary/cache
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  # Clear URL Cache
  Clears all cached Cloudinary URLs from memory.
  
  **Access:** Restricted 🔒
  **Route:** `DELETE /api/cloudinary/cache`
  
  Useful when images are updated on Cloudinary and you need to force 
  regeneration of URLs with new timestamps.
}
