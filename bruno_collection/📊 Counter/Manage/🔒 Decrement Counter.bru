meta {
  name: 🔒 Decrement Counter
  type: http
  seq: 3
}

post {
  url: {{base_url}}{{endPointApi}}/counter/decrement/THEME_RAND
  body: none
  auth: inherit
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response success is true", function() {
    if (res.getStatus() === 200) {
      const body = res.getBody();
      expect(body.success).to.be.true;
      expect(body.message).to.equal("Success");
    }
  });
  
  test("New counter value is returned", function() {
    if (res.getStatus() === 200) {
      const body = res.getBody();
      expect(body.data).to.exist;
      expect(body.data).to.have.property('counterValue');
      expect(body.data.counterValue).to.be.a('number');
    }
  });
  
  test("Cannot decrement below 0", function() {
    const status = res.getStatus();
    if (status === 400) {
      const body = res.getBody();
      expect(body.message).to.include("cannot decrement below 0");
    }
  });
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # 🔒 ⬇️ Decrement Counter
  
  **Description:**
  Atomically decreases a Redis counter by 1.
  Prevents decrementing below 0 (returns error if already at 0).
  
  ⚠️ **Restriction:** This endpoint requires authentication.
  
  ---
  
  ### 📥 Request (Input)
  
  **Method:** `POST`
  **URL:** `{{base_url}}{{endPointApi}}/counter/decrement/:name`
  
  **Authentication:**
  * 🔒 **Required**: `Authorization` Header (Bearer Token)
  
  **URL Parameters:**
  | Param | Type | Required | Description |
  | :--- | :--- | :---: | :--- |
  | `name` | String | ✔️ | Counter name (e.g., THEME_RAND) |
  
  **Query Parameters:**
  * None
  
  **Body:**
  * No body required.
  
  ---
  
  ### 📤 Response (Output)
  
  **Success Response (200 OK):**
  ```json
  {
    "success": true,
    "message": "Success",
    "data": {
      "counterName": "THEME_RAND",
      "redisKey": "app:counter:theme_rand",
      "counterValue": 42,
      "exist": true
    },
    "timestamp": "2026-01-19T18:39:21.316Z"
  }
  ```
  
  **Counter Already at 0 (400 Bad Request):**
  ```json
  {
    "success": false,
    "message": "Cannot decrement counter below 0",
    "timestamp": "2026-01-19T18:39:21.316Z"
  }
  ```
  
  **Invalid Counter Name (400 Bad Request):**
  ```json
  {
    "success": false,
    "message": "Counter name is required and must be valid.",
    "timestamp": "2026-01-19T18:39:21.316Z"
  }
  ```
  
  **Unauthorized (401):**
  ```json
  {
    "success": false,
    "message": "Unauthorized",
    "timestamp": "2026-01-19T18:39:21.316Z"
  }
  ```
  
  ### Notes
  
  - Operation is atomic and thread-safe
  - Counter cannot go below 0 (safety feature)
  - Requires admin authentication for security
}
