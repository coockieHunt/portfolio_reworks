import {COLOR as MainColor}  from '../../config'

export const COLOR = {
    primary: MainColor.primary,
    accentuate: MainColor.accentuate,
    secondary_background: MainColor.secondary_background,
};